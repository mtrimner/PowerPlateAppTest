import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    height: 50,
    paddingHorizontal: 5,
    marginVertical: 5,
    borderRadius: 5,
    justifyContent: 'space-evenly',
    alignItems: 'center',
    borderWidth: 2,
    borderColor: '#ffffff',
  },
  loaderSection: {
    flexDirection: 'row',
  },
});
