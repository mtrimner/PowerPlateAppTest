import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    height: 50,

    borderRadius: 5,
    justifyContent: 'space-evenly',
    alignItems: 'center',
  },
  loaderSection: {
    flexDirection: 'row',
  },
});
